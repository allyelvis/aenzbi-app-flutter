import 'package:flutter/material.dart';

class AccountingModule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Accounting Module'),
      ),
    );
  }
}
