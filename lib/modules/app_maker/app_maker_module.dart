import 'package:flutter/material.dart';

class App_makerModule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('App_maker Module'),
      ),
    );
  }
}
