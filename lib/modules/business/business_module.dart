import 'package:flutter/material.dart';

class BusinessModule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Business Module'),
      ),
    );
  }
}
