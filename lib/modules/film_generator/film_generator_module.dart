import 'package:flutter/material.dart';

class Film_generatorModule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Film_generator Module'),
      ),
    );
  }
}
