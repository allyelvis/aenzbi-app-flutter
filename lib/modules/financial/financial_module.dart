import 'package:flutter/material.dart';

class FinancialModule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Financial Module'),
      ),
    );
  }
}
