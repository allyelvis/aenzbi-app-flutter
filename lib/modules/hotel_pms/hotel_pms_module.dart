import 'package:flutter/material.dart';

class Hotel_pmsModule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Hotel_pms Module'),
      ),
    );
  }
}
