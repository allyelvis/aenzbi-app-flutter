import 'package:flutter/material.dart';

class Music_generatorModule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Music_generator Module'),
      ),
    );
  }
}
