import 'package:flutter/material.dart';

class Restaurant_posModule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Restaurant_pos Module'),
      ),
    );
  }
}
