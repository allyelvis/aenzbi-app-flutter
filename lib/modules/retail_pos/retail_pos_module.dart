import 'package:flutter/material.dart';

class Retail_posModule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Retail_pos Module'),
      ),
    );
  }
}
