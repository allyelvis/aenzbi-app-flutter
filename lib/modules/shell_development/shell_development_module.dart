import 'package:flutter/material.dart';

class Shell_developmentModule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Shell_development Module'),
      ),
    );
  }
}
